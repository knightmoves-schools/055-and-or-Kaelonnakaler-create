function taxable(state,taxExempt){
  if (IA,taxExempt === false) {
return 'is taxable'
  }
}
function isVehicle(hasWheels,canFly,canSwim){
  if (true);
    return 'is vehicle'
  if (hasWheels === true, canFly, canSwim === false);
}
}
return 'is vehicle' 
if (canFly === true, hasWheels, canSwim === false);
return 'is vehicle'
if (canSwim, === true, hasWheels, canFly === false);
return 'is vehicle'
if (canSwim, canFly === true, hasWheels === false);

